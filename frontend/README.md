# Frontend workspace

This directory contains the React/Vite frontend for GolemCore Brain.

## Commands

```bash
npm install
npm run dev
npm run lint
npm run build
```

The production build is copied into Spring Boot static resources during `./mvnw package`.
