# Test-only overrides. CSRF is disabled because legacy controller tests POST through MockMvc
# without an XSRF token. The outbound guard is relaxed because some adapter tests hit local
# mock servers on 127.0.0.1.
brain.security.csrf-enabled=false
brain.outbound.allow-private-addresses=true
